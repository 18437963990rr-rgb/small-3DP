# Meteor 高速打印、层间铺粉与分段层厚测试记录 - 2026-07-14

本文档整理 2026-07-14 本轮关于 `physical_home_fast` 高速打印链路、Meteor 三 Pass 数据时序、铺粉车与墨车并行、成型缸升降、末层铺粉保护及分段层厚测试的结论。它不是逐字对话记录，而是供后续现场测试、故障回溯和继续开发使用的工程记录。

**最后更新：** 2026-07-14

---

## 1. 维护约束

本项目为老项目，存在 GBK、ANSI、UTF-8 及历史混合编码风险。本轮始终遵守以下约束：

- 只允许最小范围修改指定位置；
- 不以会重写整文件的方式保存源码；
- 不顺手修复全文件注释、格式或编码；
- 修改后检查 diff 范围和文件编码；
- 保留工作区内用户已有修改，不回退无关内容。

本轮涉及的主要源码：

```text
1-3DP主控界面(人机交互模块)\主界面.cs
1-3DP主控界面(人机交互模块)\手动操作界面.cs
1-3DP主控界面(人机交互模块)\5-SharpControl.cs
1-3DP主控界面(人机交互模块)\4-MeteorPrintEngine.cs
```

现场日志：

```text
F:\AppLog_Info_20260714_09.log
F:\AppLog_Info_20260714_10.log
F:\AppLog_Info_20260714_11.log
F:\AppLog_Info_20260714_13.log
```

---

## 2. 本轮目标

本轮目标是在稳定出图和机械安全优先的前提下，将单层打印节拍压缩至约 `30 s`，并继续评估可安全压缩的部分。

优化优先级确定为：

1. 速度优先；
2. 保证全幅、全部喷头稳定出图；
3. 图像拼接和更激进的数据链优化最后考虑；
4. 任何 Meteor 数据链异常、喷头缺图、Pass 卡住或运动状态不确定时，回退到稳定顺序。

---

## 3. 当前高速打印功能开启方式

当前快速链路仍按以下方式开启：

1. 切换到“物理 Home 快速打印”；
2. 开启“粉车回程提前 Pass0 预备”；
3. 使用 `physical_home_fast` 运动分支；
4. 保留 Meteor Pass 数据就绪门控和必要的轴位置安全检查。

相关环境状态以运行日志中的下列字段为准：

```text
METEOR_AUTO_PRINT_MOTION_BRANCH=physical_home_fast
METEOR_POWDER_RETURN_EARLY_PASS0_PREP=1
```

---

## 4. 机械动作与参数结论

### 4.1 铺粉去程中的轴8动作

用户确认轴8去程先下降、后回升的设计目的是消除机械回差。该动作不等于额外下降全部补偿量，净下降只应等于当前层厚。

当前快速链路将回差补偿由 `0.9 mm` 调整为 `0.7 mm`：

```text
去程下降 = -(层厚 + 0.7 mm)
随后回升 = +0.7 mm
净下降   = -层厚
```

例如层厚为 `0.2 mm`：

```text
下降 -0.9 mm
回升 +0.7 mm
净下降 -0.2 mm
```

该下降准备允许在触发铺粉链路时尽早并行，以减少单独等待轴8小距离运动的时间。

### 4.2 铺粉回程中的轴8动作

铺粉车回程期间的轴8避让用于防止回程压坏已铺粉床。该动作仍为：

```text
下降 -1.5 mm
到达回程安全位置后回升 +1.5 mm
净位移 0 mm
```

它是回程临时避让，不计入层厚进给。当前只将去程回差补偿改为 `0.7 mm`，没有将回程避让从 `1.5 mm` 改小。

### 4.3 当前铺粉车速度

本轮快速链路目标参数：

| 项目 | 当前设定 |
|---|---:|
| 正式铺粉速度 | `95 mm/s` |
| 铺粉车快速回程速度 | `235 mm/s` |
| 轴8去程回差补偿 | `0.7 mm` |
| 轴8回程粉床避让 | `-1.5/+1.5 mm` |

快速链路中的速度以运行时代码和配置参数为准，不应只依据手动操作界面曾显示的旧值判断。

---

## 5. 铺粉车与墨车并行的判断

用户确认：轴7到达约 `440 mm` 后，铺粉速度为约 `90-95 mm/s`；墨车回程约 `185 mm/s`。结合两车方向、位置和速度，原先担心的简单追撞模型不成立。

但并行仍需要满足以下条件：

- 墨车已离开共享危险区域；
- 铺粉车方向和目标位置正确；
- 轴7、轴8状态读取正常且无报警；
- 上一铺粉回程尾段没有残留任务；
- Pass0 真正开扫前仍需通过轴位置和 Meteor 数据门控。

`AppLog_Info_20260714_10.log` 对应的现场异常为：铺粉车与墨车回程同步启动后，铺粉车前进约 `0.5 s` 又反向返回 `112 mm`。根因方向为上一层铺粉回程尾任务与下一次 Command2 重入/重复下发发生竞争。

处理方向：

- 记录并等待上一层轴7回程尾任务；
- 上一回程未结束时禁止再次下发铺粉去程；
- 增加重复正式铺粉触发的去重；
- 超时或状态不一致时停止打印，不允许第二次粉车去程。

---

## 6. Meteor 数据链迭代与稳定性结论

### 6.1 提前 Pass0 数据准备尝试

本轮曾尝试进一步提前 Pass0 数据准备和安全释放点，以压缩 Pass0、Pass1、Pass2 切换中的等待。

现场随后出现：

- 首层在约 `525 mm` 位置长时间卡住；
- 1号喷头不出图；
- 第一层扫描结束后没有可靠进入第二层；
- 第二层 Pass2 卡住；
- Meteor 数据链可能处于文档/Pass 状态未完成或错误复用状态。

因此取消激进的 Meteor 提前提交方案，恢复稳定的单层三 Pass 出图链路，再只优化机械动作和明确安全的等待点。

### 6.2 当前稳定策略

当前保留的原则：

- Pass0 运动前需要确认相应 Meteor 文档已就绪；
- Pass1、Pass2 不采用未经现场验证的跨 Pass 预提交；
- Meteor 返回 busy 或文档未完成时采用有限重试和明确日志；
- 不为追求不足 1 s 的收益破坏全喷头、全幅和跨层稳定性；
- 喷头1缺图或 Pass2 卡住时，优先判断数据链状态，不继续放宽门控。

`AppLog_Info_20260714_13.log` 的两层测试最终成功，说明恢复后的稳定出图链路和当前机械并行方案可继续作为现场测试基线。

---

## 7. 层末提前铺粉问题及修复

### 7.1 原系统设计

原主循环明确设计为：最后一层打印完成后不再进给铺粉，避免把最终打印层埋掉。

因此，对于从已准备粉床开始的5层打印：

- 第1层使用打印前已准备好的粉床；
- 第1至第4层完成后分别铺粉，为下一层准备；
- 第5层完成后不再铺粉。

### 7.2 高速链路出现的偏差

高速链路在 Pass2 低位通过提前回调启动层末铺粉。该回调最初发生在主循环“是否最后一层”的判断之前，导致最后一层仍会提前启动一次完整铺粉。

两层成功日志中已观察到：第2层 Pass2 完成后仍启动了铺粉，并伴随面向下一层的墨车预备位置动作。

### 7.3 本轮修复

在 `主界面.cs` 做了两个最小范围修改：

1. `StartLayerEndPowderEarly()` 按本次打印区间总长度计算最终层；达到最终层时拒绝提前铺粉。
2. 主循环原末层判断改为按 `终止层 - 起始层 + 1` 计算本次区间长度，使 `6-10` 等分段打印也能在区间末层正确停止铺粉。

判断口径：

```text
本次区间循环层数 = (g_nLayerEnd - g_nLayerStart + 1) * g_nRePrintTimes
```

修复后：

- `1-5` 在第5层完成后不铺粉；
- `6-10` 在第10层完成后不铺粉；
- `11-15` 在第15层完成后不铺粉；
- 最终层不会因提前回调多下降一个层厚；
- 不再为不存在的下一层执行完整铺粉准备。

### 7.4 末层并行预备补充修复

`AppLog_Info_20260714_17.log` 进一步发现：正式铺粉回调虽然已在末层被拦截，但 Pass2 内部的 `Pass2ParallelPowderPrep` 会更早启动，仍可能让轴7从112前进到440、轴3供粉并下发轴8下降。

现已在该并行预备的启动入口增加同一打印区间判断。只有当前完成层之后确实还有下一层时，才允许启动轴7、轴8和供粉站预备；最终层只保留墨车正常回站，不再启动下一层机械预备。

---

## 8. 15层模型每5层测厚的操作方法

主界面的起始层和终止层均为1基显示，并且首尾层都包含在打印区间内。

建议分三次打印：

| 测试批次 | 起始层 | 终止层 | 实际打印层数 | 理论厚度（0.2 mm/层） |
|---|---:|---:|---:|---:|
| 第一次 | 1 | 5 | 5 | 1.0 mm |
| 第二次 | 6 | 10 | 5 | 1.0 mm |
| 第三次 | 11 | 15 | 5 | 1.0 mm |

每次启动前，日志中的内部索引应为：

```text
1-5：   g_nLayerStart=0，g_nLayerEnd=4
6-10：  g_nLayerStart=5，g_nLayerEnd=9
11-15： g_nLayerStart=10，g_nLayerEnd=14
```

### 8.1 分段之间的必要动作

修复后，每个区间最后一层不会自动铺粉。因此：

1. 打印 `1-5`，完成后直接测量第1至第5层；
2. 测量结束后，手动执行一次完整的“成型缸净下降一个层厚 + 正式铺粉”流程，形成第6层粉床；
3. 打印 `6-10`；
4. 再次测量后，手动完整铺粉一次，形成第11层粉床；
5. 打印 `11-15`，最终结束后不再铺粉。

不能只手动下降轴8，也不能只运行粉车；分段续打前需要完整的层间铺粉动作。

---

## 9. 159/160层显示规则

系统界面使用1基层号，内部数据和渲染索引使用0基层号：

```text
界面第1层   = 内部索引0
界面第160层 = 内部索引159
```

因此日志中出现 `renderIndex=159` 通常代表正在处理第160层，不代表只打印了159层。

160层任务启动时应看到：

```text
LayerStart.Text=1
LayerEnd.Text=160
g_nLayerStart=0
g_nLayerEnd=159
```

正常完成提示应为：

```text
当前打印任务完成：区间为1层到160层
```

只有当启动快照已经是 `LayerEnd.Text=159、g_nLayerEnd=158` 时，才表示本次确实只设置了159层。

---

## 10. textBox5 层厚设置在高速链路中的有效性

`textBox5` 绑定 `LayerThick`，单位为 `μm`。高速铺粉链路仍读取该参数。

设置 `0.2 mm` 层厚时，应输入：

```text
textBox5 = 200
```

高速模式轴8动作：

```text
下降 = -(textBox5 / 1000 + 0.7) mm
回升 = +0.7 mm
净下降 = -textBox5 / 1000 mm
```

因此 `textBox5=200` 时，应观察到约：

```text
下降 -0.900 mm
回升 +0.700 mm
净下降 -0.200 mm
```

### 10.1 正确设置步骤

1. 在停止打印状态下修改 `textBox5`；
2. `0.2 mm` 输入 `200`，不要输入 `0.2`；
3. 点击该参数页的“应用”；
4. 确认参数已保存至程序启动目录下的 `AutoPrint-Configuration.json`；
5. 再启动高速打印。

自动打印运动入口会重新加载 `AutoPrint-Configuration.json`。若只修改文本框但没有点击“应用”，高速链路可能继续使用上次保存值。

不建议在打印任务运行期间修改层厚。运行时参数对象会被重新加载，界面绑定对象与当前运动对象可能不同，不能保证即时修改传入正在执行的层。

---

## 11. 当前建议的现场验证顺序

1. 设置 `textBox5=200` 并点击“应用”。
2. 开启“物理 Home 快速打印”和“粉车回程提前 Pass0 预备”。
3. 先进行2层空载或低风险验证：
   - 两层全部喷头正常出图；
   - 第二层 Pass2 正常结束；
   - 最终层不再启动铺粉；
   - 轴8最终层后不再净下降一个层厚。
4. 再进行 `1-5` 测试，检查每层轴8日志是否为 `-0.9/+0.7 mm`。
5. 第5层完成后确认铺粉车未启动，再测量总厚度。
6. 手动完成一次完整层间铺粉后，设置 `6-10` 继续测试。
7. 每次启动均核对层范围快照，避免1基/0基误判。

出现以下任一情况应立即停止：

- 喷头1或其他喷头缺图；
- Pass0、Pass1、Pass2 长时间不切换；
- 铺粉车短暂前进后反向返回112 mm；
- 最终层仍触发铺粉；
- 轴8净下降量与 `textBox5` 不符；
- 上一轴7回程未结束又开始第二次去程。

---

## 12. 验证与构建状态

- 最终层保护修改后，`git diff --check` 通过；
- `主界面.cs` 文件头仍为 UTF-8 BOM（`EF BB BF`），未发生整文件编码转换；
- 完整解决方案构建仍被工程既有环境问题阻断，包括：
  - 非字符串资源需要 `System.Resources.Extensions`；
  - 部分 `obj` 下 NuGet 中间文件缺失；
  - 旧工程引用和资源序列化设置不完整；
- 上述构建问题不是本轮末层判断修改引入；
- 实机验证仍是高速运动、Meteor 数据链和最终层行为的必要验收手段。

---

## 13. 当前基线结论

本轮可继续测试的基线为：

```text
打印分支：physical_home_fast
粉车回程提前 Pass0 预备：开启
正式铺粉速度：95 mm/s
铺粉车快速回程速度：235 mm/s
轴8去程回差补偿：0.7 mm
轴8回程避让：-1.5/+1.5 mm
层厚0.2 mm时 textBox5：200 μm，并点击应用
最终层：不下降下一层、不铺下一层粉
```

当前策略以已验证的稳定三 Pass 出图链为基础，通过机械动作并行、铺粉速度和回程速度压缩节拍，不再优先采用未经验证的激进 Meteor 提前提交方案。
