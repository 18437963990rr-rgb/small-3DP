# Meteor 扫描时序与墨车/铺粉车防撞准备记录 - 2026-07-12

本文档整理本轮关于 `physical_home_fast` 扫描时长、墨车和铺粉车并行方案、防撞门控，以及现场坐标采样工具的结论。它不是逐字对话记录，而是供后续实施和实机验证使用的工程记录。

**最后更新：** 2026-07-12

---

## 1. 目标与约束

当前验收约束为整层扫描/铺粉节拍尽量压入 `<= 30 s`。铺粉车当前约 `16 s/层`；仅靠缩短 Meteor 通信等待无法满足整层节拍，必须评估墨车回清洗站与铺粉准备动作的安全并行。

安全优先级高于节拍：任何位置未知、状态读取失败、超时或报警的情况，都不得放行铺粉车进入与墨车共享的运动区域。

---

## 2. 当前基线与提速估算

根据 `F:\AppLog_Info_20260712_11.log` 的稳定轮次：

| 项目 | 当前实测/估算 |
|---|---:|
| 三次有效 X 扫描 | 约 12.45 s |
| Pass0 STARTSCAN 到 Pass2 扫描结束 | 约 16.11 s |
| physical_home_fast Pass0 开始到 Pass2 扫描结束 | 约 18.39 s |
| 墨车从等待位出发到回清洗等待位 | 约 25.09 s |
| 含 EndJob 完成 | 约 25.65 s |

当前典型速度：扫描/Pass 移动 `175 mm/s`，墨车回清洗站 `150 mm/s`。扫描物理范围约 `15 <-> 635 mm`，每 Pass 约 620 mm。

若墨车扫描、去打印位、回清洗站均提高到 `225 mm/s`，并假定加减速参数不变：

| 项目 | 估算 |
|---|---:|
| 墨车全流程 | 约 20.2-20.8 s |
| 铺粉车回程提高到 225 mm/s | 约 15.4-15.6 s |
| 两者完全串行 | 约 35.6-36.4 s |
| 仅以墨车回站阶段重叠约 5 s | 约 30.5-31.4 s |

结论：速度提高可明显接近验收线，但仍需要约 `1-1.5 s` 的安全重叠或额外非碰撞准备动作并行，才可能稳定落入 `<= 30 s`。

---

## 3. 铺粉车 200 -> 90 mm/s 的实际代码逻辑

自动打印当前使用 `NewAutoSupplyPowderThread2()`。铺粉车不是在某个写死的 `415 mm` 点从 `200` 自动改为 `90 mm/s`，而是分两段读取现场参数：

```text
第一段终点 = POWDERCAR_DROP_BEGIN - PreAngleRotatePositionForPowderSupply
           = 455 mm - 落粉提前量
第一段速度 = CureBackSpeed
第二段速度 = PowderCarBackSpeed
```

因此：

- 若现场 `CureBackSpeed=200`、`PowderCarBackSpeed=90`，即观测到的 `200 -> 90 mm/s`；
- 若 `textBox30` 的有效提前量为 `15 mm`，分段点应为 `455 - 15 = 440 mm`；
- 若实际观察到约 `415 mm`，则运行时有效提前量更接近 `40 mm`，需要以日志记录值为准；
- `textBox30` 绑定 `PreAngleRotatePositionForPowderSupply`，不仅影响落粉/超声触发，也参与第一段终点、回程轴 8 让位触发点的计算。

不能以肉眼观察的坐标作为后续防撞门控常量。

---

## 4. 并行方案的可行边界

拟议流程：Pass2 扫描到物理低端约 `15 mm` 时，墨车开始向清洗站回程；同时启动不占用共享空间的铺粉准备动作，例如粉缸/落粉、轴 3 送粉、轴 8 让位、辊筒预转。

该方案可行，但必须分级：

1. **立即允许并行：** 已确认无空间干涉的落粉、送粉、升降、辊筒准备。
2. **受控预运动：** 铺粉车只可移动至共享区域外的安全等待点。
3. **主行程位置互锁：** 仅在墨车实际编码器位置、运动方向、铺粉车位置及两侧状态均满足防撞条件时，才允许铺粉车进入共享区域。

`DocsQueuedLane1 >= 1` 是 Meteor 的下一 Pass 数据就绪门控，仍必须保留；它不应承担墨车/铺粉车防撞职责。

在 `225 mm/s` 下，应用层 `20 ms` 轮询对应约 `4.5 mm` 位移，不能作为最后一道安全保护。最终实施应由运动控制器/PLC 或等效的底层互锁承担共享区域限入；C# 负责流程编排、超时、日志和默认保守回退。

---

## 5. 本轮新增的防撞坐标采样工具

修改文件：

```text
1-3DP主控界面(人机交互模块)\手动操作界面.cs
```

### 5.1 位置

在手动操作界面的 `tabPage3`，即“原位固化增强控制”页右侧留白区域，新增：

```text
记录防撞坐标
```

同页原“固高/Meteor 双光栅坐标采集（测试）”面板会覆盖该区域，当前已仅在 UI 中临时隐藏；采样代码仍保留，未删除。

防撞坐标按钮会显式启用并置于当前页最上层。点击入口先写入 `[CollisionSafetySnapshot] record button click received`，用于区分“界面点击未到达”和“控制器坐标读取失败”。

按钮只读取控制器反馈，不会下发任何运动命令。

### 5.2 每次记录内容

日志前缀：

```text
[CollisionSafetySnapshot]
```

采集项：

- 墨车轴 1：显示 X 坐标、原始编码器计数、轴状态；
- 墨车轴 2：显示 Y 坐标、原始编码器计数、轴状态；
- 铺粉车轴 7：显示坐标、原始编码器计数、轴状态；
- `textBox30` 当前文字值和实际生效的提前量；
- 计算后的第一/第二段切换点；
- `CureBackSpeed` 与 `PowderCarBackSpeed` 实际值。

同时修正了手动操作页原“粉车位置”标签仍读取旧轴 4 的问题，现改为读取自动铺粉实际使用的轴 7。

### 5.3 现场采样步骤

1. 停止自动打印、自动铺粉、自动清洗和固化线程。
2. 将墨车缓慢移动至 Pass2 低端的物理位置，当前暂按约 `15 mm`。
3. 将铺粉车以低速逐步靠近，达到人工确认的最近安全位置后完全停止。
4. 点击“记录防撞坐标”。
5. 从该位置退出 `10-20 mm` 后再记录至少两组，作为安全裕量参考。
6. 提供包含 `[CollisionSafetySnapshot]` 的应用日志，用于建立坐标映射和门控条件。

单个“极限位置”记录只能作为边界参考，不应直接作为自动放行阈值；最终门控须在实测边界基础上加入车体外廓、制动距离、控制周期和编码器误差裕量。

---

## 6. 后续实现建议

在取得现场坐标样本并完成机械确认前，不启用墨车回站与铺粉车主行程的自动并行。

后续最小修改方案：

1. Pass2 结束时异步启动无碰撞的铺粉准备动作。
2. 墨车回清洗站的同时，铺粉车在安全等待点保持等待。
3. 依据实际编码器建立 `WaitingForInkCarClear` 状态。
4. 满足安全位置、方向、速度、状态、无报警条件后，才释放铺粉车主行程。
5. 任一读取失败/超时/状态异常时，禁止放行并回退到现有串行流程，同时写日志。

---

## 7. 验证状态

- `git diff --check` 已通过。
- 未修改 Meteor 的 `DocsQueuedLane1` 门控，未启用新的自动并行链路，未增加环境变量要求。
- 已尝试 `dotnet build "LaserAdd_3DPrinting V2.0.sln" --no-restore`；构建被工程既有问题阻断，包括 `obj` 下 NuGet 生成文件缺失、资源序列化配置和旧引用警告，未出现本轮新增坐标采样代码的编译报错。

---

## 8. 首次现场防撞坐标采样 - 2026-07-13

日志：`F:\AppLog_Info_20260713_15.log`

在 `2026-07-13 15:09:45.410` 已确认按钮点击和控制器读取均成功：

```text
[CollisionSafetySnapshot] local=2026-07-13 15:09:45.410
inkX=94.696mm, rawEnc1=94696.0, sts1=0x200
inkY=251.310mm, rawEnc2=251310.0, sts2=0x200
powderAxis7=315.342mm, rawEnc7=315342.0, sts7=0x200
preAngleUi='15', preAngleEffective=15.000mm
powderStageSwitch=440.000mm
stage1CureBackSpeed=200.000mm/s
stage2PowderBackSpeed=90.000mm/s
```

结论：

- 防撞坐标记录按钮、点击事件和轴 1/2/7 的读取均正常；
- 现场 `textBox30=15` 已真正生效，第一/第二段速度切换坐标为 `440 mm`，不是 `415 mm`；
- 该样本的墨车 X 是 `94.696 mm`，不是计划中 Pass2 低端的 `15 mm`。它可作为当前人工确认的空间边界样本，但不能直接替代“墨车已到 Pass2 低端”的运动条件；
- 轴状态均为 `0x200`，且采样前对应 JOG 已停止，日志中未见限位位 `0x20/0x40`，可作为静止测量样本保存。

后续至少还需记录两组样本：从该边界退出 `10-20 mm` 的保守位置，以及墨车实际处于 Pass2 低端时对应的铺粉车安全位置。完成后才能建立自动并行的保守门控阈值。
